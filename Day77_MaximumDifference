from os import *
from sys import *
from collections import *
from math import *

def maximumDifferece(n, arr):
    # Write your code here.
    m=max(arr)
    n=min(arr)
    p=' '
    if (m-n)%2==0:
        p="EVEN"
    else:
        p="ODD"
    return p
