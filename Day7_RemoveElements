class Solution {
    public int removeElement(int[] nums, int val) {
        int writePointer = 0;
        for(int i: nums){
            if(i!=val){
                nums[writePointer]=i;
                writePointer++;
            }
        }
        return writePointer;
    }
}
